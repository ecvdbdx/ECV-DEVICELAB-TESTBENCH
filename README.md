# ECV-DEVICELAB-TESTBENCH
A test bench to tryout the device lab at ECV

## Server setup

1. Install local dependencies
  ```
  npm install
  ```

2. Launch the server
  ```
  npm run start
  ```
>>>>>>> Adds basic BrowserSync setup
